# Plugins

Plugins use the shared SRIC plugin manifest and declared permissions. No plugin may bypass Scope Engine or Policy Engine. The CLI can inspect the local registry without auto-executing untrusted plugin code. Future installation/verification flows must verify package provenance and permissions before activation.
