# AI

AI is optional and disabled by default. The deterministic core remains functional without an AI provider. Future AI adapters must treat retrieved content as untrusted data, preserve Claim-Evidence contracts, support local/cloud/hybrid modes, and pass versioned evals before release. AI output cannot mark a finding VALIDATED.
