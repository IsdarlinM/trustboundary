# Release

Release gates: unit/integration/E2E/security tests, Ruff, strict mypy, clean install, upgrade path, CLI help coverage, Web API/UI checks, secret scan, dependency review, SBOM/checksums/signatures where release infrastructure is configured. Generated binaries are not committed to source.
