# Installation

Use `scripts/install-linux.sh` or `scripts/install-windows.cmd`. Install SRIC Core first when it is not available from a configured package source. Verify with `trustboundary doctor`.
