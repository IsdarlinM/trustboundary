# Architecture

TrustBoundary Mapper is a local-first product built on SRIC concepts: evidence/provenance, explicit claim states, scope/policy gates, isolated workspaces and AI-disabled-by-default operation. Product-specific state is stored as validated JSON in each workspace.
